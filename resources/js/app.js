
import 'bootstrap/dist/css/bootstrap.min.css'; // Optional if not using Laravel CSS
import 'bootstrap'; // This automatically pulls Popper from @popperjs/core if installed
import '../css/app.css'; // Your custom or Tailwind CSS

// Optional: Make Bootstrap available globally (if needed for inline scripts)
import * as bootstrap from 'bootstrap';
window.bootstrap = bootstrap;